<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>
<META HTTP-EQUIV="Refresh" content="3">
<div class="container">
<h2>Database Results</h2>
this page Refresh every 3 seconds to make sure users get the latest data.<br>

<ul>
  <table border="1" cellspacing="0" cellpadding="0">
    <tr>
      <td>name</td>
      <td>size</td>
      <td>height</td>
      <td>type</td>
      <td>delete</td>
    </tr>
    <% rows.forEach(function(r) { %>
      <tr>


        <td><%= r.name %></td>
        <td><%=r.size%></td>
        <td><%=r.height%></td>
        <td><%=r.type%></td>
        <td>
        <button type="button" id="<%= r.name %>">delete</button>
        </td>
      </tr>
    <% }); %>
  </table>
<style media="screen">
table {
   border-collapse: collapse;
   font-family: Futura, Arial, sans-serif;
}
caption {
   font-size: larger;
   margin: 1em auto;
}
th,td {
   padding: .65em;
}
th,td {
   border-bottom: 1px solid #ddd;
   border-top: 1px solid #ddd;
   text-align: center;
}
</style>
</ul>

</div>

</body>
<script>
  var butArr=document.getElementsByTagName('BUTTON');
  for (var i = 0, len = butArr.length; i < len; i++) {
    butArr[i].onclick=function(){
      window.open("/del_person/" +this.id);
  };
  }

</script>
</html>
